//Find lcm of two numbers.
#include <stdio.h>
int main()
{
int a ,b ;
int i=2;
printf("Enter two numbers:");
scanf("%d%d",&a,&b);

while(i<=(a*b))
{
if(i%a==0&&i%b==0)
{
printf("LCM=%d",i);

}
i=i+1;
break;
}

return 0;
}
