// Input a no. and print its reverse.
#include <stdio.h>
int main()
{
int n;
printf("Enter no.");
scanf("%d",&n);
int t=n;
int rev=0;
while(n>0)
{
int d = n%10;
 rev = rev*10+d;
n=n/10;
}
if(rev==t)
printf("Palindrome");
else
printf("Not palindrome");
return 0;
}
